# Module: state-backend

This module will contain the AWS S3 + DynamoDB backend resources for Terraform
remote state and state locking.

Currently a skeleton only — actual resources will be added in Phase 5.

Files:
- `main.tf` – module logic (future resources)
- `variables.tf` – input variables
- `outputs.tf` – module outputs (placeholders)
